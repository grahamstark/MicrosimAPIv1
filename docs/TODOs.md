# TODOs

1. `working_dir` needs set to the thing used in `MicroVisualisations.phunpackify`. Meantime, it's recoverable from the `phunpack` results entry.

2. Run name needs set somewhere, not `sb_017`.

3. Compute the parameter hash in julia, not pgsql, pass the value to the run routine, OR add hash to run record and, again, pass to run routine so use it in directory names.

4. Add a logger X
